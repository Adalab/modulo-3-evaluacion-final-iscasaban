import FilterName from './FilterName';
import FilterGender from './FilterGender';

const Filter = () => {
  return (
    <section>
      <form>
        {/* <FilterName />
        <FilterGender />
         */}
      </form>
    </section>
  );
};

export default Filter;
